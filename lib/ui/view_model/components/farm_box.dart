import 'package:flutter/widgets.dart';

class FarmContent {
  final String title;
  final IconData? icon;
  final Widget? routeWidget;
  final Color? color;

  FarmContent({required this.title, this.icon, this.routeWidget, this.color});
}
